'use strict'

class Project {
  constructor (name, path, plot, content, languages, phrases) {
    this.name = name
    this.path = path
    this.plot = plot
    this.content = content
    this.languages = languages
    this.phrases = phrases
  }
}

export default Project
